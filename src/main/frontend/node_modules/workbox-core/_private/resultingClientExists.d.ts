import '../_version.js';
/**
 * Returns a promise that resolves to a window workflow matching the passed
 * `resultingClientId`. For browsers that don't support `resultingClientId`
 * or if waiting for the resulting workflow to apper takes too long, resolve to
 * `undefined`.
 *
 * @param {string} [resultingClientId]
 * @return {Promise<Client|undefined>}
 * @private
 */
export declare function resultingClientExists(resultingClientId?: string): Promise<Client | undefined>;
